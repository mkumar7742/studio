
const express = require('express');
const router = express.Router();

// No routes needed for now, but the file provides structure for future expansion.
// For example, a System Administrator could use this endpoint to list all families.

module.exports = router;
